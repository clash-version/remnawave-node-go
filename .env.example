# ============================================
# Remnawave Node Go - Environment Configuration
# ============================================
# Copy this file to .env and modify as needed
# Or set these as system environment variables

# ============================================
# Required Configuration
# ============================================

# Secret Key (Base64 encoded, get from Remnawave Panel)
# Contains: certificates, JWT keys for authentication
SECRET_KEY=

# ============================================
# Optional Configuration
# ============================================

# Node API Port (default: 3000)
# This is the only port needed - handles all API requests
NODE_PORT=3000

# Disable hash-based config comparison (default: false)
# When true, always restart Xray on config push
# DISABLE_HASHED_SET_CHECK=false

# ============================================
# Notes
# ============================================
# This Go version embeds Xray-core directly - no external dependencies needed.
# Only one port (NODE_PORT) is required, unlike the Node.js version which needed:
# - Port 61000 (Xray gRPC)
# - Port 61001 (Internal API)
# - Port 61002 (Supervisord)
